{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @hybrid-remote-components/* --parallel start",
    "build": "lerna run --scope @hybrid-remote-components/* build",
    "serve": "lerna run --scope @hybrid-remote-components/* --parallel serve",
    "clean": "lerna run --scope @hybrid-remote-components/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
